#include <stdio.h>

int main()
{
    int a = 0;

    // printf("%i %i %i %i\n", a++, a--, a--, a++);

    // a++;
    printf("%2i\n", a++);

    // a--;
    printf("%2i\n", a--);

    // a--;
    printf("%2i\n", a--);

    // a++;
    printf("%2i\n", a++);

    // a = 0;
    // printf("%i %i %i %i\n", ++a, --a, --a, ++a);

    return 0;
}